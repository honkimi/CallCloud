

<ul class="nav nav-tabs tel-tab" style="margin-bottom: 15px;">
  <li data="/tels/id" class=""><a href="#home" data-toggle="tab">Operator</a></li>
  <li data="/tels/id/records" class="active"><a href="#home" data-toggle="tab">Record</a></li>
  <li data="/tels/id/edit" class=""><a href="#home" data-toggle="tab">Settings</a></li>
</ul>


<div id="records">
  <table class="table table-striped table-hover ">
    <thead>
      <tr>
        <th>#id</th>
        <th>From</th>
        <th>to</th>
        <th>Voice</th>
        <th>Duration</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
    <% @records.each do |record| %>
      <tr>
         <td><%= record.id %></td>
         <td><%= record.from %></td>
         <td><%= record.to %></td>
         <td><audio src="<%= record.voice_url %>" controls></audio></td>
         <td><%= record.duration %></td>
         <td><%= record.updated_at %></td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <% if @records.empty? %>
    <p class="lead">おっと、まだ一つも録音データが無いようです。設定の Is Record にチェックが入っているかを確認してください。</p>
  <% end %>
</div>

<script>

</script>
